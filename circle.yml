machine:
  ruby:
    version: 2.3.3 
compile:
  override:
    - bash circleci-helpers/compile.bash
deployment:
  production:
    branch: master
    commands:
      - bash circleci-helpers/deployment.bash
  development:
    branch: development
    commands:
      - bash circleci-helpers/deployment.bash