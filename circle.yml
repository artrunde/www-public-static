machine:
  ruby:
    version: 2.3.3 
compile:
  override:
    - bundle exec middleman build --verbose --environment=development
deployment:
  production:
    branch: master
      commands:
        - bash circleci-helpers/upload.bash development
  development:
    branch: development
      commands:
        - bash circleci-helpers/upload.bash development


     
