machine:
  ruby:
    version: 2.3.3 
compile:
  override:
    - bundle exec middleman build --verbose --environment=$CIRCLE_BRANCH
deployment:
  commands:
    - bash circleci-helpers/upload.bash $CIRCLE_BRANCH

     
